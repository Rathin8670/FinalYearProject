version: "3.8"

services:

  backend:
    build: ./backend
    container_name: carepulse-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env

  ai-service:
    build: ./ai-doctor-service
    container_name: carepulse-ai
    ports:
      - "8000:8000"
    env_file:
      - ./ai-doctor-service/.env

  videocall:
    build: ./videocall
    container_name: carepulse-videocall
    ports:
      - "4000:4000"
    env_file:
      - ./videocall/.env